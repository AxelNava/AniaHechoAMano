{
  "name": "artisan-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:studio": "bun --filter @artisan/api db:studio"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
